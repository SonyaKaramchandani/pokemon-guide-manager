/** @jsx jsx */
import React from 'react';
import { jsx } from 'theme-ui';

export const DebugContainer4BdPanel = ({ children }) => (
  <div sx={{ width: '370px', height: '100vh', p: '10px' }}>{children}</div>
);

export const DebugContainer350 = ({ children }) => (
  <div sx={{ width: '370px', p: '10px' }}>{children}</div>
);
