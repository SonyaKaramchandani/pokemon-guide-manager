import React from 'react';
import { action } from '@storybook/addon-actions';
import SortBy from './SortBy';

export default {
  title: 'SortBy'
};

export const text = () => <SortBy>Hello SortBy</SortBy>;
