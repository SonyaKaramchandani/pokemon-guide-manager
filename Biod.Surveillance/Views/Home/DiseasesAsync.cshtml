@model IList<Biod.Surveillance.ViewModels.DiseaseClass>
@{
    ViewBag.Title = "Diseases";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<p>Use this area to provide additional information.</p>

<h2>Diseases</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>

    @using (Html.BeginForm())
    {
        <p>
            Select Department: @Html.DropDownList("SelectedEvents", "All")
            <input type="submit" value="Filter" />
        </p>
    }*@

<table class="table">
    @*<tr>
            <th>
                @Html.DisplayNameFor(model => model.CourseID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Credits)
            </th>
            <th>
                Department
            </th>
            <th></th>
        </tr>*@
   

    @foreach (var item in Model)
    {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiseaseId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiseaseName)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DiseaseId }) |
                @Html.ActionLink("Details", "Details", new { id = item.DiseaseId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DiseaseId })
            </td>
        </tr>
    }

</table>
