@using System.Configuration;
<div class="container-fluid">
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @* @Html.ActionLink("Application name", "Index", "Home", null, new { @class = "navbar-brand" })*@
            </div>
            <div class="navbar-collapse collapse">
                <ul id="currentUsers" class="nav navbar-nav">
                    @Html.ActionLink("Surveillance", "Index", "Home", null, new { @class = "navbar-brand", id = "surveillanceLogo" })
                    @*<li>@Html.ActionLink("Home", "Index", "Home")</li>*@
                    <li>@*@Html.ActionLink("Contact", "Contact", "Home")*@</li>
                    @*@if (Request.IsAuthenticated)
                        {
                            <li>@Html.ActionLink("Chat", "Chat", "Home")</li>
                        }*@
                    @*<li id="currentUsers" style="color:crimson"></li>*@
                    @if (Request.IsAuthenticated && (User.IsInRole(ConfigurationManager.AppSettings.Get("AdminUsersRole"))))
                    {
                        <li>@Html.ActionLink("RolesAdmin", "Index", "RolesAdmin")</li>
                        <li>@Html.ActionLink("UsersAdmin", "Index", "UsersAdmin")</li>
                    }
                    else if (Request.IsAuthenticated && (User.IsInRole(ConfigurationManager.AppSettings.Get("PowerUsersRole"))))
                    {
                        <li>@Html.ActionLink("UsersAdmin", "Index", "UsersAdmin")</li>
                    }

                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
</div>

