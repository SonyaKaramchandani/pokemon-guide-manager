@model IEnumerable<Biod.Surveillance.Models.HamType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HamTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HamTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HamTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HamTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HamTypeId })
        </td>
    </tr>
}

</table>
